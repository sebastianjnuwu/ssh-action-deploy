name: Action 
on: [push]

jobs:
  build:
    name: ✨
    runs-on: ubuntu-latest
    steps:
      - name: 🙄
        uses: actions/checkout@v3
      - name: ✍️
        uses: ./
        env:
          IP: ${{ secrets.IP }}
          USER: ${{ secrets.USER }}
          KEY: ${{ secrets.KEY }}
          RUN: ls; pwd; uptime 
          
